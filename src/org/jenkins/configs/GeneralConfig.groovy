package org.jenkins.configs

class GeneralConfig implements Serializable {
    // For re-use cache in docker build
    static final String DOCKERFILE_BUILD_AGENT = "docker-build-agent-94-94" // agent name for build docker image

}